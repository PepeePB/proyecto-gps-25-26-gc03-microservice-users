spring.datasource.url=jdbc:mysql://db:3306/MusicFly?allowPublicKeyRetrieval=true&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.data.redis.host=redis
spring.datasource.hikari.initialization-fail-timeout=60000
spring.datasource.hikari.connection-timeout=60000

# ==== Configuración de Eureka Server ====
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
# Mirar a ver como se adapta esto a produccion
eureka.instance.hostname=localhost
